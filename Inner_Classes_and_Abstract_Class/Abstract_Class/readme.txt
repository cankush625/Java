Here, Animal.java is the base abstract class which have two methods eat() and breathe()
Bird.java is also an abstract class which is implemented from the Animal.java class. It have one extra method fly().

canFly.java is an interface which implements fly() method.

Dog.java and Parrot.java are the methods extended from respective base classes.

Animal_main.java is the main java class.